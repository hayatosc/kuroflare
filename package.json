{
  "name": "kuroflare",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "typecheck:compat": "pnpm -r typecheck:compat",
    "lint": "./node_modules/.bin/oxlint .",
    "lint:types": "./node_modules/.bin/oxlint --type-aware .",
    "format": "./node_modules/.bin/oxfmt .",
    "format:check": "./node_modules/.bin/oxfmt --check ."
  },
  "devDependencies": {
    "@typescript/native-preview": "7.0.0-dev.20260615.1",
    "oxfmt": "^0.55.0",
    "oxlint": "^1.70.0",
    "oxlint-tsgolint": "^0.23.0",
    "tsdown": "^0.22.2",
    "vitest": "^4.1.9"
  },
  "packageManager": "pnpm@11.2.0"
}
